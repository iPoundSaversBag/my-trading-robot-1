# Example environment template (do NOT put real secrets in version control)
# Copy to .env locally and fill in real values for local testing only.
# Production / CI values go in GitHub repository Secrets and Vercel project env vars.

BOT_SECRET=
VERCEL_URL=
BINANCE_API_KEY=
BINANCE_API_SECRET=
# Optional Telegram notification settings
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
